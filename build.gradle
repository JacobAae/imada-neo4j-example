version 'unspecified'

apply plugin: 'groovy'
apply plugin: 'application'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.4.15'

    compile 'org.neo4j:neo4j:3.1.0'
}

run {
    main = 'net.grydeske.Neo4jExample'
    standardInput = System.in
}